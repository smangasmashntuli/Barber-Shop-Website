name: Deploy SMASH Barbershop to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: vue-app/package-lock.json
      - name: Install dependencies
        working-directory: vue-app
        run: npm ci
      - name: Verify booking and calendar logic
        working-directory: vue-app
        run: npm run verify
      - name: Build
        working-directory: vue-app
        run: npm run build
      - name: Add SPA fallback
        working-directory: vue-app
        run: cp dist/index.html dist/404.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: vue-app/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4